# yonder-site

The new yonderartland.com — Astro 4, Tailwind, React islands, Supabase, Resend.

## Setup

```sh
nvm use            # Node 22
pnpm install
cp .env.example .env   # then fill in keys
pnpm dev
```

## See also

- `CLAUDE.md` — project context, stack, migration plan
- `../yonder-reference/` — archive of the previous WordPress site (SQL dump, image archive, partial migration)
